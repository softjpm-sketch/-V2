# PRD — 병원 마케팅 채널 진단 모듈 (petamos ‘마케팅 채널 점검’)

> 이 문서는 Claude Code에 넘겨 바로 구현을 시작하기 위한 제품 사양서입니다.
> **핵심 원칙: 이미 완성된 진단 엔진(`clinic-audit/report_generator.py`, `criteria.json`)은 수정하지 말고 그대로 재사용**하고, 그 위에 ‘입력 → AI 분석 초안 → 사람 검수 → 리포트 발행/저장’ 레이어만 새로 만든다.

---

## 1. 목적 / 배경
어떤 동물병원이든 온라인 채널(홈페이지·네이버 블로그·인스타·네이버 플레이스·카카오톡 채널·카카오맵·T맵) + **AEO·GEO(AI 검색 노출)**를 같은 기준으로 진단하고, 개선안을 **실행 우선순위 매트릭스**로 뽑아주는 기능을 petamos ‘마케팅 채널 점검’ 페이지에 넣는다.

이미 검증된 것:
- 진단 엔진: 입력 JSON(`config.*.json`) → `build_html(cfg, criteria)` → 완성 HTML 리포트(채널 진단 + 우선순위 매트릭스).
- 실제로 인천·부평 두 병원을 이 엔진으로 산출 완료(각 8개 진단 섹션).

새로 만들 것: 위 엔진에 **재료(링크·캡처)를 넣으면 AI가 입력 JSON을 채워주고, 담당자가 검수·수정 후 발행**하는 콘솔.

## 2. 대상 사용자 & 핵심 시나리오
- **주 사용자**: 내부 마케팅 담당자(비개발자). (2차: 외부 병원에 제공하는 SaaS로 확장 가능하게 설계)
- **핵심 시나리오**:
  1. 담당자가 새 병원의 이름·주소·전화를 입력하고, 링크 4종(홈피·인스타·카카오맵·T맵)을 붙이고, 캡처 3종(네이버 블로그·플레이스·카카오톡)을 업로드한다.
  2. 시스템이 링크 채널은 자동 수집, 캡처 채널은 이미지에서 정보를 읽어, **AI가 채널별 점수·강점·약점·개선안(입력 JSON)을 초안으로 생성**한다.
  3. 담당자가 초안을 화면에서 검토·수정한다(점수 슬라이더, 문구 편집).
  4. ‘리포트 생성’ → 엔진이 HTML 리포트 + 우선순위 매트릭스 출력. 저장·공유·PDF.
  5. 병원별 진단 이력이 쌓이고, 나중에 재점검·지점 비교가 가능하다.

## 3. 범위 (Phase)
**Phase 1 (MVP — 이번 구현 목표)**
- ‘마케팅 채널 점검’ 페이지: 병원 목록 + 신규 진단 마법사(입력→AI 초안→검수→발행).
- 링크 채널 자동 수집(홈피·인스타·카카오맵·T맵) + 캡처 업로드(네이버·카카오톡).
- Claude API로 채널별 입력 JSON 초안 생성(비전으로 캡처 판독 포함).
- 기존 엔진으로 리포트 생성·미리보기·저장(병원별 JSON 파일 + 산출 HTML).
- AEO·GEO 모듈(준비도 자동 평가 + 노출 질의 세트 캡처 업로드).

**Phase 2**
- DB 전환(파일 → RDB), 진단 이력·버전 관리, 지점 비교 뷰.
- 링크 채널 수집 고도화(스케줄 재점검), 개선안 담당자·기한 체크리스트.

**Phase 3**
- 외부 병원 대상 SaaS(로그인·권한·과금), 대시보드/알림.

**Out of scope(명시)**: 네이버·카카오의 무단 자동 스크래핑(약관 위반) — 이 채널들은 **담당자 캡처 업로드**로만 처리한다.

## 4. 아키텍처
```
[브라우저 UI]  ── 입력/업로드/검수 ──►  [petamos 백엔드(파이썬)]
                                          │
              ┌───────────────────────────┼────────────────────────────┐
              ▼                           ▼                            ▼
     [수집기 collectors]          [AI 분석 analyzer]            [리포트 엔진(기존)]
   - 링크: 헤드리스로 텍스트/메타 추출   - Claude API 호출            - report_generator.build_html
   - 캡처: 업로드 이미지 저장           - criteria.json 관점 주입       - criteria.json (수정 금지)
                                      - 스키마 강제로 config JSON 생성
              │                           │                            │
              └──────────► [config JSON(초안)] ──검수──► [config JSON(확정)] ──► [리포트 HTML]
                                          │
                                     [저장소] configs/<병원>.json, output/<병원>.html (Phase2: DB)
```

## 5. 데이터 흐름
1. **입력 수집**: 폼 데이터 + 링크 + 업로드 이미지.
2. **채널 원재료 확보**:
   - 링크 채널 → 서버에서 헤드리스(Playwright 등)로 렌더 후 텍스트·메타·핵심 요소 추출(홈피: title/meta/H1/tel/이미지alt/구조화데이터 유무 등).
   - 캡처 채널 → 이미지 파일 보관(+ 필요 시 Claude 비전으로 판독).
3. **AI 분석**: 원재료 + `criteria.json`의 해당 채널 관점을 Claude에 주고, **정해진 스키마의 config JSON을 출력**하게 함(아래 8절).
4. **사람 검수**: 초안 화면에서 점수·문구·개선안 수정.
5. **리포트 생성**: 확정 config → `build_html` → HTML 저장/미리보기.
6. **저장·이력**: 병원별 config·리포트 보관.

## 6. 데이터 모델
**입력 JSON 스키마는 기존 것을 그대로 사용**한다(`examples/config.template.json`, README ‘입력 JSON 구조’ 참고). 요약:
- `clinic`: name, tagline, url, date, analyst, channels_analyzed[], disclaimer
- `combined`(선택): summary, strengths[], weaknesses[], actions[]
- `channels[]`: `type`(homepage/blog/instagram/naverplace/kakao/map/tmap/aeo_geo/generic), scores[{name,value,note}], criteria_results[{key,verdict}], strengths[/weaknesses[]{title,body,severity}], actions[{title,effort,impact,cost}]

**저장(Phase 1)**: `configs/<clinic_slug>.json`(입력 확정본), `output/<clinic_slug>.html`(리포트), `uploads/<clinic_slug>/...`(캡처).

**DB 스키마 초안(Phase 2)**:
- `clinic(id, name, slug, address, phone, hours, created_at)`
- `audit(id, clinic_id, created_at, status[draft|reviewed|published], config_json, report_html_path)`
- `audit_action(id, audit_id, channel_label, title, effort, impact, cost, tier, feasibility, owner, due_date, done)` — 매트릭스를 실행 체크리스트로 확장.

## 7. 화면 정의 (‘마케팅 채널 점검’)
1. **병원 목록**: 진단한 병원 카드(이름·마지막 진단일·종합 상태). ‘새 진단’ 버튼.
2. **신규 진단 — 입력**: 병원 기본정보 폼 + 채널 입력(링크 붙여넣기 / 캡처 드래그 업로드) + ‘없음’ 체크. (가이드 문구는 접수 체크리스트 재사용)
3. **AI 초안 — 검수**: 채널별 카드로 점수(슬라이더)·강점/약점·개선안(효과/노력/비용) 편집. ‘재분석’ 버튼(특정 채널만 다시).
4. **리포트**: 생성된 HTML 미리보기(iframe) + 발행/다운로드(HTML·PDF)/저장.
5. (Phase2) **비교 뷰**: 지점·기간 비교, 실행 체크리스트.

## 8. AI 연동 상세 (Claude API)
- **역할**: 채널 원재료(텍스트/메타/캡처) + 해당 채널의 `criteria.json` 관점을 받아, **그 채널의 scores·criteria_results·strengths·weaknesses·actions를 스키마대로 출력**.
- **구현**: Anthropic Messages API. 출력은 **JSON 스키마를 강제**(tool use/structured output 또는 “아래 스키마의 JSON만 출력” 지시 + 검증·재시도)로 받아 파싱 실패를 없앤다.
- **비전**: 네이버·카카오 캡처는 이미지로 첨부해 리뷰 수·별점·정보·소식 등을 읽게 한다.
- **채널별 1회 호출**(또는 소량 병렬) → 결과를 합쳐 `channels[]` 구성. `combined`는 전 채널 확정 후 한 번 더 호출(공통 강점/과제/개선안 + NAP 대조).
- **effort/impact/cost는 AI가 1~3으로 제안**하되, 최종은 담당자 검수값. 우선순위·티어 계산은 엔진(`score_action`)이 담당(중복 계산 금지).
- **프롬프트 골격**(참고):
  > 너는 병원 온라인 채널 진단가다. 아래 [채널 유형]과 [평가 관점(criteria)]에 따라 [원재료]를 분석해, 지정된 JSON 스키마로만 출력하라. 점수는 0~100, verdict는 관점별 판정, 강점/약점은 근거 기반 1~2문장, 개선안은 effort/impact/cost(각1~3)와 함께. 확인 불가한 항목은 추정임을 명시하고 verdict를 ‘확인 필요’로.
- **환각 방지**: “원재료에 없는 수치는 지어내지 말 것, 불명은 ‘확인 필요’”를 시스템 프롬프트에 명시. 담당자 검수 단계가 최종 안전장치.

## 9. 채널별 수집 방법 (매트릭스)
| 채널 | 수집 | 비고 |
|---|---|---|
| 홈페이지 | 링크 자동(헤드리스) | title·meta·H1·tel·alt·구조화데이터·SPA 여부 추출 |
| 인스타그램 | 링크 자동(공개 프로필 메타) | 팔로워/게시물/바이오; 상세는 캡처 보완 |
| 카카오맵 | 링크 자동 | 주소·전화·영업시간·사진 수·후기 on/off |
| T맵 | 링크 자동 | 이름·주소·전화·핀 |
| 네이버 블로그 | **캡처 업로드** | 자동 접근 차단(약관) |
| 네이버 플레이스 | **캡처 업로드** | 별점·리뷰수·정보·소식 |
| 카카오톡 채널 | **캡처 업로드** | 친구수·소식·쿠폰·상담버튼 |
| AEO·GEO | 준비도 자동 + 노출 **캡처 업로드** | 아래 10절 |

## 10. AEO·GEO 모듈
- **준비도(자동)**: 홈페이지·블로그 원재료로 구조화데이터·FAQ·엔티티 일관성(전 채널 주소/전화 대조 → NAP 불일치 자동 플래그)을 AI가 평가.
- **노출(반자동)**: 병원 정보로 **대표 질의 세트를 자동 생성** →
  - 브랜드 질의: `{병원명}`
  - 의도 질의: `{지역} 24시 동물병원`, `{지역} {핵심진료(MRI 등)} 동물병원 추천`, `{지역} {증상} 잘하는 동물병원`
  - 담당자가 각 질의를 구글/생성엔진에 넣고 결과를 **캡처 업로드** → AI가 “등장 여부·순위·무엇을 인용(플레이스/블로그/제3자)·경쟁·구글 리뷰 평점”을 판독.
- **산출**: `type:"aeo_geo"` 채널로 점수·강점/약점·개선안 생성(예: 구조화데이터 삽입, FAQ 신설, 제3자 디렉터리 관리, NAP 통일, 생성엔진 월간 추적).
- **주의**: 생성엔진은 로그인·차단·변동이 있어 ‘경향’으로 해석. 자동 크롤 금지 채널은 캡처로만.

## 11. 실행가능성·우선순위 규칙 (기존 유지)
`report_generator.py`의 `score_action()`을 그대로 사용:
```
우선순위 점수 = 효과×3 − 노력 − 비용
바로: 점수≥5 또는 (노력=1 & 효과≥2) / 중기: 2~5 미만 / 장기: 2 미만
실행가능성: 쉬움(노력≤1,비용≤1)/보통(노력≤2,비용≤2)/어려움(그 외)
```
규칙 변경이 필요하면 이 함수 한 곳만 수정.

## 12. 비기능 요구
- **컴플라이언스**: 네이버·카카오 자동 스크래핑 금지 → 캡처 업로드. robots·약관 준수.
- **개인정보**: 리뷰 캡처 등에 개인정보가 포함될 수 있으니 저장·표시 최소화, 접근 제어.
- **비용/성능**: 채널당 1 AI 호출 기준, 병렬·캐시로 1병원 진단 수십 초 내. 링크 수집 타임아웃·실패 시 캡처 대체 안내.
- **정확성 표기**: 화면·리포트에 “참고용 진단, 사실관계는 실제 정보로 확정” 고지 유지(엔진에 이미 있음).

## 13. Claude Code 착수 지시 (그대로 수행)
1. petamos의 실제 프레임워크·라우팅·템플릿 위치를 먼저 파악한다.
2. `clinic-audit/`를 프로젝트에 포함(또는 패키지로 import 경로 설정)하고, `report_generator.build_html`·`criteria.json`을 **그대로 재사용**한다(수정 금지).
3. ‘마케팅 채널 점검’에 4개 화면(목록·입력·검수·리포트)을 추가한다(7절).
4. 수집기(collectors): 링크 채널 헤드리스 추출 + 캡처 업로드 저장(9절).
5. analyzer: Claude API로 채널별 config JSON 초안 생성(8절, 스키마 강제·비전·환각방지).
6. 저장: `configs/<slug>.json` + `output/<slug>.html`(Phase1). 검수 후 확정본으로 `build_html` 호출해 리포트 저장.
7. AEO·GEO 모듈(10절): 질의 세트 자동 생성 + 캡처 업로드 판독.
8. `examples/config.sky.json`·`config.bupyeong.json`으로 **전체 플로우 E2E 동작을 실제로 띄워 확인**(초안→검수→리포트).
9. API 키·비밀은 환경변수. 네이버·카카오 자동 스크래핑은 구현하지 않는다.

## 14. 수용 기준(Acceptance)
- 담당자가 링크4+캡처3을 넣고 **10분 내 리포트 발행**까지 완료된다.
- 생성된 리포트에 8개 진단 섹션(7채널 + AEO·GEO)과 우선순위 매트릭스가 나온다.
- AI 초안이 스키마 오류 없이 렌더되고, 검수에서 점수·문구·개선안을 수정하면 리포트에 반영된다.
- 네이버·카카오는 캡처로만 처리(자동 스크래핑 없음).
- 기존 엔진 파일(`report_generator.py`·`criteria.json`)은 변경되지 않았다.

## 15. 로드맵 요약
- **Phase 1**: 위 MVP.
- **Phase 2**: DB·이력·비교·실행 체크리스트.
- **Phase 3**: 외부 병원 SaaS(로그인·권한·과금)·정기 재점검 알림.

## 16. 모듈: 리뷰 평판 분석 (Review Reputation)
채널 ‘점검’과 별개로, **리뷰 내용(텍스트)을 읽어 평판을 분석**하는 모듈. (‘리뷰 수·별점’은 채널 점검에서, ‘리뷰 내용 분석’은 이 모듈에서 담당 — 두 모듈은 별도 실행·저장)

- **입력**: 네이버 플레이스·구글(·후기 켠 카카오맵)의 리뷰 텍스트. 네이버·카카오는 자동수집 불가(약관) → **붙여넣기 또는 캡처 업로드(비전 판독)**. 구글은 가능하면 수집, 아니면 붙여넣기.
- **AI 처리(Claude API)**: 리뷰별 감성 라벨(긍/중/부) → 채널별 감성 분포 집계, 칭찬/불만 **키워드·테마 빈도**, **대표 인용** 추출(짧게), 최신성·재방문 신호, **사장님 답글 운영 여부**, **채널 간 평판 격차**(예: 네이버 긍정 vs 구글 저평가) 진단. 스키마 강제 출력·환각 방지(원문에 없는 내용 금지, 불명은 표기).
- **출력 스키마(review_report)**:
  - `channels[]`: `{name, review_count, rating, recency, sentiment:{pos,neu,neg}}`
  - `praise_keywords[]` / `complaint_keywords[]`: `{keyword, weight}`
  - `quotes[]`: `{text, channel, author, sentiment}`
  - `findings[]`: `{title, body, severity}`
  - `actions[]`: `{title, effort, impact, cost}` — 기존 `score_action()`로 우선순위 계산 재사용
- **렌더링**: 기존 `report_generator.py`에 리뷰용 렌더 함수(감성 막대·키워드·인용 카드)를 추가하거나 리뷰 전용 템플릿을 둔다. CSS·실행가능성 로직은 재사용. (구현 예시는 `output/`의 리뷰 리포트 산출물 참고 가능)
- **화면**: ‘리뷰 평판’ 탭 — 리뷰 텍스트/캡처 업로드 → AI 감성·키워드 초안 → 담당자 검수 → 리포트 발행.
- **주의**: 리뷰는 작성자 고유 콘텐츠 → 인용·활용 시 동의 권장, 개인정보·진료정보 최소화. 부정 리뷰 대응은 신중히(사실확인).
- **수용 기준**: 리뷰 텍스트를 넣으면 채널별 감성 분포 + 칭찬/불만 키워드 + 대표 인용 + 개선안이 리포트로 나오고, 네이버 vs 구글 같은 **채널 평판 격차**가 드러난다.

---
### 부록: 참고 파일
- `report_generator.py` — 엔진(`build_html`, `score_action`)
- `criteria.json` — 채널 유형·관점(aeo_geo 포함)
- `examples/config.template.json` — 입력 스키마 예시
- `examples/config.sky.json`, `examples/config.bupyeong.json` — 실제 산출 예시(각 8섹션)
- `INTEGRATION_python.md` — 파이썬 통합 코드(iframe/서버 생성)
- `output/clinic-audit-webapp.html` — 브라우저 단독 웹 도구(임베드/참고용)
