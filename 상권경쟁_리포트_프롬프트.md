# 상권·경쟁 리포트 재현 프롬프트 (Claude Code 붙여넣기용)

> 아래 **프롬프트 전체를 복사**해서 맨 위 `■ 입력값`만 병원에 맞게 채운 뒤 Claude Code에 붙여넣으면,
> Petamos 상권·경쟁 리포트와 **동일한 방법론·동일한 디자인의 HTML 리포트**가 생성됩니다.
>
> - **A안(권장·정확)**: `~/petamos` 저장소가 있으면 그대로 진단 엔진을 돌려 실데이터로 뽑습니다.
> - **B안(포터블)**: 저장소 없이도, 프롬프트에 내장된 공식·디자인 규칙만으로 리포트를 생성합니다.

---

## ▶ A안 — Petamos 엔진으로 실데이터 생성 (저장소 있을 때)

```
너는 Petamos 병원 진단 어시스턴트다. 아래 병원의 상권·경쟁 리포트를 생성해라.

■ 입력값
- 병원명: {{예: 부평스카이동물의료센터}}
- 주소: {{예: 인천 부평구 …}}
- 병원 종류(tier): {{1차 | 2차}}
- (선택) 소상공인 상권정보: 월매출 {{만원}}, 일유동인구 {{명}}, 상권유형 {{주거 밀집형|주거·상업 혼합|상업 중심}}
- (선택) 주차 가능 {{Y/N}}, 아파트 밀집 {{Y/N}}

■ 작업
1. `~/petamos`에서 가상환경(.venv)으로 진단을 실행한다:
   - `trade_area.analyze(주소, accessibility, monthly_sales, daily_footfall, area_type)`
   - `competition.analyze(주소, 병원명, radius_m=(1차 1000 / 2차 3000), subject_tier=tier)`
   - 이를 `diagnose.run(...)`로 통합해 result dict를 만든다.
2. `axis_report.build_trade(result)` → 상권 리포트 HTML,
   `axis_report.build_competition(result)` → 경쟁 리포트 HTML을 생성한다.
3. 두 파일을 `reports/{{병원명}}_상권분석리포트.html`, `reports/{{병원명}}_경쟁분석리포트.html`로 저장하고,
   Playwright(python .venv)로 렌더해 깨짐 없이 나오는지 확인한 뒤 핵심 수치(포화도 등급·동급 경쟁/의뢰처 수·co_density)를 요약 보고한다.

※ report_generator.py·criteria.json은 수정하지 말 것. 리뷰/채널은 자동 크롤링하지 말 것(약관·차단).
```

---

## ▶ B안 — 포터블(저장소 없이 공식만으로 생성)

```
너는 동물병원 상권·경쟁 분석가다. 아래 입력값과 규칙에 따라, 지정된 구조·디자인의 HTML 리포트 2개
(상권 분석 / 경쟁·의뢰처 분석)를 생성해라. 근거 없는 수치는 지어내지 말고, 모르는 값은 '미입력'으로 둔다.

■ 입력값
- 병원명 / 주소 / tier(1차·2차)
- 시군구 가구수·인구, 지역 동물병원 수, (가능하면) 등록 반려동물수
- 최근접 지하철역·거리(m)
- 반경 내 동물병원 목록: [{이름, 카테고리, 거리m}]  ← 카카오/지도에서 수집한 목록 붙여넣기
- (선택) 월매출·일유동인구·상권유형, 주차·아파트밀집

■ 상권 분석 규칙
- 반려가구 = 가구수 × 0.28
- 시장 포화도 = 반려가구 ÷ 병원수  (등급: A≥1500 여유·블루오션 / B≥1100 양호 / C≥800 보통 / D<800 포화)
- 역세권 도보분 = 역거리 ÷ 67, 800m 이내면 역세권
- 지표 점수(0~100)와 가중치:
  · 시장규모·포화도(30): 포화도 2000→90/1500→80/1100→66/800→52/500→42/그외33
  · 유동인구(20): 25만→90/15만→80/8만→68/4만→55/2만→45/그외38
  · 입지·접근성(20): 40 + 20×(주차+역세권+아파트밀집)
  · 소득(15): 월매출 6000→90/5000→80/4500→68/3500→55/2500→45/그외38
  · 상권유형(15): 주거밀집85/혼합68/상업52
- 축 점수(20점 만점) = 측정된 지표만 가중평균 × 0.2, 충실도% 병기

■ 경쟁 분석 규칙
- 반경: 1차=1km, 2차=3km
- 주변 병원 1·2차 자동 분류: 이름·카테고리에 [메디컬센터/의료원/24시/응급/2차/레퍼럴/전문동물/대학동물 …] 신호가 있으면 2차, 없으면 1차
- 역할: 대상과 같은 tier=경쟁, 다른 tier=의뢰처
  · 대상 1차 → 주변 1차=경쟁, 2차=의뢰처
  · 대상 2차 → 주변 2차=경쟁, 1차=의뢰처(유입원·가점)
- 자기 병원(이름 일치 또는 ≤30m)은 목록에서 제외
- 거리 가중 w = exp(-거리/450)
- 경쟁압력 = Σw(경쟁), 의뢰처풀 = Σw(의뢰처)
- 경쟁여유 base = 100 × exp(-경쟁압력/2.5)
- 의뢰처 가점 = (2차만) min(20, 의뢰처풀×4)
- co_density(경쟁 포지션) = clamp(base + 가점, 10, 100)
- 거리밴드 ~300m/300~600m/600m~, 리뷰 정밀비교 Top3 = 경쟁 중 거리가중 상위 3

■ 리포트 구조
- 상권: ①스냅샷 ②종합진단(지표카드)+"포화도란?" 박스 ③분석방법 ④유리한점 ⑤유의점 ⑥로드맵(바로/중기/장기)+정리
- 경쟁: ①스냅샷(+2차면 핵심 한 줄) ②(2차만)1·2차 개념+흐름도 ③종합진단 ④주변병원 지형(동급/의뢰처 표) ⑤유리한점 ⑥로드맵+정리

■ 디자인(HTML, self-contained, 인쇄→PDF 가능)
- 헤더: 네이비 그라디언트(#152B54→#20406f), 흰 제목 + pill 형태 메타 배지
- 본문: 연회색 배경(#f6f8fb), 흰 카드(radius 14px, 옅은 그림자), 섹션 번호 뱃지(네이비 사각)
- 점수 바: 우수(초록#2E8B6B ≥70) / 양호(주황#C58A22 ≥45) / 개선(빨강#C0453B)
- 스냅샷 4칸 그리드, 표는 하늘색(#e8f0f9) 헤더
- 각주: "반려가구=양육률28% 추정, 등록수 개 위주 하한값, 1·2차 명칭 기반 추정, 점수는 상대평가" 명시

먼저 상권 리포트, 다음 경쟁 리포트 순으로 완성하고, 각 리포트의 핵심 수치를 한 줄로 요약 보고해라.
```

---

## 참고
- 방법론 상세: `상권경쟁_분석방법론.md`
- 실제 생성기 코드: `petamos/hospital/axis_report.py` (`build_trade` / `build_competition`)
- 디자인 토큰·점수 임계값은 위 프롬프트 값과 코드가 1:1로 일치한다.
