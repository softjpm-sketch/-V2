# Petamos 상권·경쟁 분석 방법론

> 동물병원 진단의 5대 축 중 **① 상권 분석**과 **② 경쟁 분석**을 우리가 실제로 어떻게 계산·평가·리포트화하는지 정리한 문서입니다.
> (근거 코드: `petamos/hospital/trade_area.py`, `competition.py`, `axis_report.py`, `indicators.py`, `scoring.py`)

---

## 0. 공통 원칙

- **각 축 20점 만점**, 5축 합계 100점. 지표별 0~100 점수를 **가중평균 → ×20**으로 축 점수를 만든다.
- **결측 내성(graceful degradation)**: 측정된 지표만으로 축 점수를 내고, `충실도%`(coverage = 측정 가중 ÷ 전체 가중)를 함께 표기한다. 데이터를 더 넣을수록 충실도·정밀도가 올라간다.
- **종합 등급컷**: `A ≥ 80 · B ≥ 65 · C ≥ 50 · D < 50`.
- **자동 크롤링 금지**: 네이버·인스타 등은 약관·차단 이슈로 자동 수집하지 않는다. 리뷰·채널은 담당자 복붙/캡처만 사용. (상권·경쟁은 공공데이터·카카오 로컬 API로 자동)

---

## 1. 상권 분석 (Trade Area)

### 1-1. 입력
| 구분 | 항목 | 출처 |
|---|---|---|
| 필수 | 병원 주소 | 담당자 입력 |
| 자동 | 시도/시군구, 최근접 지하철역 | 카카오 지오코딩 |
| 자동 | 시군구 인구·가구수 | 통계청 SGIS |
| 자동 | 등록 반려동물수(실측) | 농림축산검역본부 |
| 자동 | 지역 동물병원 수 | 내장 병원 데이터(reference) |
| 선택(수동) | 주차·아파트밀집 여부 | 담당자 체크 |
| 선택(수동) | 월평균 추정매출·일유동인구·상권유형 | 소상공인 상권정보 PDF |

### 1-2. 핵심 계산식
```
반려가구(추정)   = 시군구 가구수 × 양육률 0.28        # PET_OWNERSHIP_RATE
시장 포화도       = 반려가구 ÷ 지역 병원수             # 전국평균 ≈ 1,100
등록기반 포화도   = 등록 반려동물수 ÷ 병원수           # 실측 병행(개 위주 하한값)
역세권 도보시간   = 최근접역 거리(m) ÷ 67(m/분)
역세권 여부       = 최근접역 ≤ 800m (도보 약 12분)
```

### 1-3. 시장 포화도 등급 (블루/레드오션)
| 포화도(반려가구/병원) | 등급 | 라벨 |
|---|---|---|
| ≥ 1,500 | **A** | 여유 · 블루오션 |
| ≥ 1,100 | **B** | 양호 |
| ≥ 800 | **C** | 보통 |
| < 800 | **D** | 포화 · 레드오션 |

> 포화도가 **클수록** 병원 한 곳이 나눠 갖는 반려가구가 많다 = 시장 여유(블루오션).

### 1-4. 지표별 점수(0~100)와 가중치
| 지표 | 가중 | 산식 |
|---|---|---|
| `ta_pet_households` 시장규모·포화도 | **30** | 포화도 기반: 2000→90 / 1500→80 / 1100→66 / 800→52 / 500→42 / else 33. (병원수 없으면 반려가구 규모로: 5만→88 / 3만→80 / 1.5만→68 / 5천→54 / else 40) |
| `ta_footfall` 유동인구·상권활력 | 20 | 일유동: 25만→90 / 15만→80 / 8만→68 / 4만→55 / 2만→45 / else 38 |
| `ta_accessibility` 입지·주차·역세권 | 20 | 40 + 20 × (주차 + 역세권 + 아파트밀집), 각 0/1 |
| `ta_income` 소득·소비력 | 15 | 월매출(만원): 6000→90 / 5000→80 / 4500→68 / 3500→55 / 2500→45 / else 38 |
| `ta_area_type` 상권유형 | 15 | 주거 밀집형 85 / 주거·상업 혼합 68 / 상업 중심 52 |

> 반려동물은 **정주 주거인구**가 핵심 수요라 주거형 상권을 높게 본다.

### 1-5. 상권 리포트 구성
① 상권 스냅샷(반려가구·등록수·병원수·포화도등급) → ② 종합 진단(지표 카드, 측정된 것만) + **"시장 포화도란?" 설명 박스** → ③ 어떻게 분석했나(4개 데이터 소스) → ④ 유리한 점(데이터 기반 findings) → ⑤ 유의점·기회 → ⑥ 활용 전략 로드맵(바로/중기/장기) → 정리.

---

## 2. 경쟁 분석 (Competition)

### 2-1. 핵심 전제 — 1차·2차와 '경쟁 vs 의뢰처'
동물병원은 **1차(동네 병원)–2차(전문 의료센터)** 구조로 협업한다. 1차가 고난도 케이스(CT·MRI·중환자·전문수술)를 2차로 **의뢰(refer)**하고, 치료 후 1차로 회송한다.

따라서 주변 병원은 **진단 대상 병원의 tier에 따라 정반대**로 갈린다:
- **대상이 1차** → 주변 **1차 = 직접 경쟁**, 2차 = 의뢰처(내가 환자를 보냄)
- **대상이 2차** → 주변 **2차 = 직접 경쟁**, **1차 = 의뢰처(나에게 환자가 유입)** ← 많을수록 **기회(가점)**

> 대상 병원의 tier는 **원장이 직접 선택**한다. 주변 병원의 1·2차는 **명칭·카테고리 텍스트의 신호로 자동 추정**한다.

### 2-2. 입력
| 항목 | 값 |
|---|---|
| 주소·병원명 | 담당자 입력 |
| 대상 tier | 원장 선택(1차/2차) |
| 반경 | **1차 = 1,000m · 2차 = 3,000m** (자동) |
| 병원 목록 | 카카오 로컬 API "동물병원" 키워드 검색(반경 내) — 리뷰·평점 미포함 |

### 2-3. 2차 신호(TIER2_SIGNALS) — 주변 병원 자동 분류
명칭·카테고리에 아래 키워드가 **하나라도** 있으면 2차로 추정, 없으면 1차:
`메디컬센터 · 의료원 · 동물메디컬 · medical · 24시 · 응급 · 야간 · 종합동물 · 2차 · 레퍼럴/referral · 전문동물 · 대학(부속)동물 · 수의대` 등.

### 2-4. 핵심 계산식
```
거리 가중 w(d)   = exp(-d / 450)        # 0m=1.0, 450m≈0.37, 900m≈0.14 (가까울수록 위협↑)
동급 경쟁 압력    = Σ w(d)  over 동급(경쟁) 병원
의뢰처 유입 풀    = Σ w(d)  over 타tier(의뢰처) 병원

경쟁 여유(base)  = 100 × exp(-경쟁압력 / 2.5)          # 압력 0 → 100점
의뢰처 가점       = (2차만) min(20, 의뢰처풀 × 4)        # 1차는 0
co_density       = clamp(base + 가점, 10, 100)          # 경쟁 포지션 종합
```
- **자기 병원 제외**: 이름 일치, 또는 좌표 ≤30m 최근접 1곳(카카오 등록명이 다를 때 대비).
- **거리 밴드**: 동급 경쟁을 `~300m / 300~600m / 600m~`로 분포화.
- **리뷰 정밀비교 Top3**: 동급 경쟁 중 거리 가중 상위 3곳 자동 추천.
- **특화 빈자리**: SPECIALTY_KEYWORDS(치과·안과·심장·영상·24시 등) 중 동급 경쟁이 커버 못 한 항목 = 기회. (단, 병원명에 키워드가 있을 때만 신뢰 → 없으면 결측 처리)

### 2-5. 지표별 가중치
| 지표 | 가중 | 신뢰 조건 |
|---|---|---|
| `co_density` 경쟁 포지션(밀도) | **30** | 항상 신뢰 |
| `co_rel_position` 상대 포지션(리뷰수·평점 백분위) | 30 | 리뷰 입력 시(2차 반자동) |
| `co_gap_specialty` 빈자리(희소 특화) | 25 | 병원명 키워드 있을 때만 |
| `co_profile` 경쟁사 프로필(과목/24시) | 15 | 병원명 키워드 있을 때만 |

### 2-6. 경쟁 리포트 구성
① 경쟁 스냅샷(동급 경쟁·의뢰처·최근접·포지션) + (2차면) **핵심 한 줄** → ② (2차면) 개념 설명 + 1차↔2차 흐름도 → ③ 종합 진단(경쟁여유·의뢰처유입·리뷰포지션·종합) → ④ 주변 병원 지형(동급/의뢰처 표, 상위 5곳 + "외 N곳") → ⑤ 유리한 점 → ⑥ 경쟁 우위 로드맵 → 정리.

---

## 3. 산출물

두 축 모두 **진단만 하면 병원 무관하게 자동 생성**되는 HTML 리포트다(네이비 `#152B54`/블루 카드 디자인 통일).
- 앱: `현재 마케팅 채널 점검` → 진단 실행 → **리포트 다운로드**에서 상권/경쟁 리포트 버튼.
- 코드: `axis_report.build_trade(result)` · `axis_report.build_competition(result)`.

> 점수식은 v1 휴리스틱이며 상대 평가다. 반려가구는 양육률 28% 추정치, 등록수는 개 위주 하한값, 1·2차 분류는 명칭 기반 추정임을 리포트 각주에 명시한다.
